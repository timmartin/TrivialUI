"""Some mock objects for PySide, with just enough detail to get the
docs build to operate correctly.

Just using unittest.mock isn't enough, because we need to inherit from
some classes in the PySide library.

"""
